{
  "name": "mcp-file-organizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Demo MCP Server + Client/Agent cho use case AI File Organization Assistant",
  "scripts": {
    "generate-mock": "tsx client/generate-mock-inbox.ts",
    "agent": "tsx --env-file-if-exists=.env client/client.ts",
    "build": "tsc",
    "test": "tsx --test tests/server.test.ts"
  },
  "dependencies": {
    "@google/genai": "^2.17.0",
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
